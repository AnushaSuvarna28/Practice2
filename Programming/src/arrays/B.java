package arrays;

public class B 
{
	public static void main(String[] args) {

		arr [ ] a={1,2,3}
		a[0]=a[0]+a[1]
		a[last()]=a[last()]+a[last()-1]
		For(int i=0;i<a.length-1;i++){
		If(i>=1||i<=a.length-2)
		   a[i]=a[i-1]+a[i+1];
	}
}
